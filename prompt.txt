ere the lis to fthings you need to change:
- remove the table identifiers_with_names
- add the name for the indiovudals in the table iodentifiers and add the name of the identifiers_with_names
- and add the full url that links to the identifiers website
(use RUST to do it)
- For nationalitites, the wikidata_id disapread, find it again.
- add the dezscription_en of the natuionality
- Add the instance of the natuionality
- for the cities
- create a colum country, with the id, the name, the iso_a3, the continent
- add the descriptio of the occupations
- find the isntance of the occupations
- add a tbale wth all the definiton of the propertues that appear in the database
- create a sitelinks tables with the wikidata_id, the name, like in the sample_db (exactly like)



Here are the things to do:
- the scripts that extracts data should be independent form the scritps thta manipulates the sqlite3 database
- When extyracting data, use the QLEVER API
- When handling the tables of the salite3 datbaase, alwyas use RUST-
- The script to extarct data should be in exytraction/
- The scripts that insets data should be in database_integration/
- separate the different actiosn in different scripts
- always save thze data as json in all_humans/
- g i give you a list of instrcution, save them somewere in a text file and then do it one by one or group some if they can't be grouped

